# OAuth token không được lưu trong file này. Token do rclone giữ trong config/rclone.conf.
accounts: []

policy:
  switch_at_percent: 90
  quota_refresh_hours: 6
  retry_seconds: [60, 300, 900, 3600]
  remote_root: CameraBackup
