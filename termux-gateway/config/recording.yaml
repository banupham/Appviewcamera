recording:
  root: recordings
  format: mp4
  keep_uploaded_local_hours: 12
  failed_file_days: 7
  pre_record_seconds: 10
  post_record_seconds: 30

retention:
  mode: HYBRID
  cleanup_start_percent: 90
  cleanup_target_percent: 80
  preferred_retention_days: 30
  minimum_retention_days: 7
